DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
source devel/setup.bash
export GAZEBO_PLUGIN_PATH=$DIR/build/table_tennis:$GAZEBO_PLUGIN_PATH
export GAZEBO_MODEL_PATH=$DIR/src/table_tennis/models:$GAZEBO_MODEL_PATH
